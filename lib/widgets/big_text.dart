

import 'package:flutter/material.dart';

class BigText extends StatelessWidget {
  final Color color;
  //pass color variable
  final String text;
  //pass in the text to be writtern
  double size;
  //for the dynamic text size

  const BigText({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(

    );
  }
}